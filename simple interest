#include <stdio.h>
int main() 
{
    float p,t,r,si;
    printf("enter the value of p,t and r");
    scanf("%f%f%f",&p,&t,&r);
    si=p*t*r/100;
    printf("the result is %f",si);
    return 0;
}
output
enter the value of p,t and r10
2
3
the result is 0.600000
